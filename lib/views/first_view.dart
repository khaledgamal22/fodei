import 'package:flutter/material.dart';
import 'package:mataam_app/views_body/first_view_body.dart';

class FirstView extends StatelessWidget {
  const FirstView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: FirstViewBody(),
    );
  }
}